// Utility functions (shared)

const Utils = {
    sleep: (ms) => new Promise(r => setTimeout(r, ms)),

    // Future: DOM Selector generator
    generateSelector: (element) => {
        // Implementation TODO
        return "";
    }
};
